
import { NavDropdown } from 'react-bootstrap';

const Build = (

) => {

    return (
        <NavDropdown title="Build" menuVariant="dark">
            <NavDropdown.Item>Export</NavDropdown.Item>
        </NavDropdown>
    );
};

export default Build;